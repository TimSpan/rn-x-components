# 2026-09-13

## X-Components 组件库大版本开发（新 12+ 组件 + 全量暗黑/i18n + npm + 文档站）

### 基建
- theme.ts v2：lightTokens/darkTokens 双套 token（XTheme 类型 Widen 拓宽字面量），useXTheme()/useXThemeScheme()/setXThemeMode('light'|'dark'|'system')，xTheme 旧导出保留兼容（=浅色）。
- XLocale：轻量 i18n（zustand + zh-CN/en-US 字典 + 模板插值 {n}），useXLocale()/xT()/setXLocale()；星期/月份本地化（X_WEEKDAYS/xMonthTitle）。
- 新装依赖：@shopify/react-native-skia 2.6.2、expo-audio ~57.0.5、expo-file-system、expo-image-manipulator、expo-image-picker（**原生模块，需重建 dev client**）。

### 新组件（全部 useXTheme + i18n，示例注册在 src/examples/advanced.tsx）
- XCarousel（ScrollView 分页+克隆首尾无限循环+自动播放拖动暂停）
- XTabs（line/button、swiper 联动、lazyload、徽标、下划线 Reanimated）
- XElevator（SectionList + 右侧导航条 responder 定位）
- XDropdownMenu（XTopView 宿主 + measureInWindow 定位 + 快照激活态 + 下拉动画；MenuWithItem.Item 挂载）
- XCalendar + XCalendarPopup（duxui 逻辑 TS 重写：day/week/scope/multiple、min/max、disabledDate；弹窗底部 XPullView，取消/确定对齐 XPickerDate）
- XRecord（expo-audio：useAudioRecorder + useAudioRecorderState 轮询计时；AudioStatus 播放字段是 playing 不是 isPlaying，RecordingStatus 无 durationMillis——SDK 57 实测）
- XNumberKeyboard + XNumberKeyboardPopup（纯键盘 + 弹出模式，random/extraKey）
- XLicensePlate（省份→字母→alnum 三段键盘，新能源 8 位末位限 D/F，末位特殊字符港澳学领警挂）
- XChart（SVG 自研 XLineChart/XBarChart/XPieChart，用户选定方案：不引第三方图表库）
- XSignature/core.tsx（共享内核：PanResponder 采点+中点二次贝塞尔+transformCharacters）+ XSignatureSkia（逐字，changhu 迁移，弹层换 XPullView、按钮换 XButton、onExport(base64) 解耦上传）+ XSignature（普通连笔）
- XUpload 适配器体系：types/adapters（createMinioPresignedAdapter XHR PUT、createFormDataUploadAdapter、createMockUploadAdapter）/provider（setXUploadAdapter 全局 + XUploadProvider 局部）/helpers（pickImageFiles/pickVideoFile/compressImage/uploadBase64 用 expo-file-system 新 File API）/XUploadImage（列宽实测+串行上传累加器防旧值）/XUploadVideo

### 存量适配（子代理完成）
- 27 个存量组件全部 useXTheme 化 + 文案 i18n；模块级样式带色的移入组件内；新增 7 个 i18n key（pleaseInputLabel/pleaseSelectLabel/cannotBeEmpty/reset/expandSearch/collapseSearch/operationIrreversible）。

### npm 包
- tsconfig.lib.json（module esnext + moduleResolution bundler → ESM；nodenext 会因 zoom-toolkit 无扩展名 d.ts 导入假报 TS2305）
- npm/package.template.json（react-native-x-components，peer: react/rn/reanimated/safe-area/svg；skia、expo-* 等设 optional peers；deps: dayjs/zustand）+ npm/README.md + scripts/build-lib.sh
- .lib-build/ 已产出：62 JS + 62 d.ts，main dist/index.js。**待用户提供 npm Access Token 后 publish**（包名 react-native-x-components）。

### 图标
- scripts/gen-x-icons.py（Pillow 几何绘制）：assets/icons/ 下 x-icon-1024（圆角主图，app.json 已指向）、x-icon-square-1024、x-icon-transparent-512、x-icon-{foreground,background,monochrome}-1024、x-logo.svg + x-logo-mark.svg（官方矢量）、alt/ AI 备选稿
- 安卓 mipmap 全密度 webp 已直接替换（48/72/96/144/192 + adaptive 三层 + monochrome），iOS 待下次 prebuild 生效

### 文档站
- /Users/myx/Desktop/rn-x-components-docs（Next.js App Router 静态导出 + SEO），进行中

### 待办
- [x] **npm 发布成功**（2026-09-13 16:24）：react-native-x-components@0.1.0 已上线，registry 验证通过。过程：账号未配 2FA 被 npm 新政策拦发 → 官网开 2FA（Google Authenticator TOTP）→ npm login → npm publish --otp 成功。**教训**：无 2FA 账号 bypass token 会被降级为 staging-only；新包首次发布必须本人交互式完成。
- [ ] 用户侧安全清理：npmjs.com 上 revoke 聊天里出现过的两个旧 Token；重新生成 recovery codes（曾在聊天中明文出现）
- [ ] 用户 ~/.npmrc 现在是本人 web 登录凭据；以后发新版：改 npm/package.template.json 版本 → zsh scripts/build-lib.sh → npm publish /abs/.lib-build --otp=码
- [ ] 文档站部署前把 lib/docs-data.ts 的 SITE_URL 换成正式域名
- [ ] 重建 dev client（新原生依赖生效）
- [ ] MinIO 适配器接口约定如与 changhu 不同需改适配器入参

## 用户实战反馈修复（2026-09-13 18:xx 段）

- **XElevator**：`CompMeta` 新增 `noScroll` 字段；详情页 [name].tsx 命中时跳过 ScrollView，避免 SectionList 嵌套告警。右侧导航从单一 responder 追踪改为「每字母 Pressable（tap 跳转）+ 全局 responder（拖动定位）」双通道，可靠响应。
- **App 图标**：webp/PNG/SVG 资源全部就位；用户需 `npx expo prebuild --clean && npx expo run:android` 让 adaptive icon 路径从 app.json 重新生成到 AndroidManifest。
- **XDropdownMenu** 触发条 height=0 bug：XDropdownMenuItem 渲染时 ctx.triggerHeight 已注入但 height={0} 写了硬编码 —— 改 height={ctx.triggerHeight}。
- **XSignatureSkia 签名纸不显示**：弹层容器 `height:'88%'` 在 bottom 模式下高度链断裂（Animated.View 高度未定，inner wrapper 取不到父高）；改为固定 520 像素，signArea 也给 height: 320。
- **XUpload 加 ActionSheet**：`XActionSheet/global.tsx` 新增命令式 API（showXActionSheet 返回 Promise）；挂载点嵌入 `XPopupProvider`；`takePicture/takeVideo` helpers 加好；图点击走 `XImagePreviewService`、视频点击走新组件 `XVideoPreview`（expo-video）；上传失败 Alert 提示。
- **XRecord 录音权限**：DemoPage 加「权限配置提醒」段（注明 prebuild 自动注入 + 手填 microphonePermission 的方式），同时 `npm/README.md` 增加权限说明段。
- **3 切换按钮 + about tab**：
  - 新增 `XTheme/BrandColor.ts`（4 套品牌色预设 + zustand store + 命令式 setXBrandByName）+ `XTheme/ThemeControls.tsx`（3 个紧凑按钮）。
  - `useXTheme()` 把品牌色注入到 token 覆盖 colorPrimary/colorPrimaryActive/colorPrimaryBg。
  - 介绍页顶部、组件页 Stack headerRight、关于页顶部 三个位置都接入。
  - 底部 tab `我的 → 关于`（app-tabs.tsx）。
- **首页**：`npm 发布计划` 卡换成 `已发布到 npm 🎉` 卡，含包名/版本/安装命令/主页链接；组件清单扩到 33 个（v2 新增都补上）。
- **日历多选**：XCalendarDemo 加 multiple 段。
- **npm README**：补 react-native-gesture-handler 推荐安装 + Android/iOS 权限说明。

## 第二轮真机实测修复（glm-5.3-flash，adb 全程验证）

用户连真机（M332BF，Android 17，设备深色模式）实测，发现首轮修复大量无效。逐项修复并在真机上截图验证：

1. **ActionSheet 点击无反应（根因）**：XActionSheet.handleSelect 先 onClose 后 onSelect，命令式 show() 的 Promise 在 onClose 时被 resolve(null) 消费 → 选什么都返回 null。修复：调换顺序（onSelect 先、onClose 后）。真机验证：弹层弹出 + 点「从相册选择」成功拉起系统相册 ✓
2. **上传加号不居中**：Pressable 内 Text 直接布局，安卓上偏下。修复：包一层 flex:1 居中 View（cellCenter）✓
3. **XElevator 仍无效（架构错误）**：SectionList 的 scrollToLocation 不可靠 + 嵌套告警。**彻底重构为 duxui 同架构**：普通 ScrollView + 每组 onLayout 记录 y + scrollTo(y) + onScroll 反推高亮 + 右侧导航「字母 Pressable（点击）+ 容器 onMoveShouldSetResponderCapture（拖动连续定位）」。真机验证：无告警、tap 字母 B 精确跳转、拖动连续定位 ✓。**教训**：onStartShouldSetResponderCapture 的 grant 在 adb input tap（0ms）下不可靠，Pressable onPressIn 可靠。
4. **Menu 样式问题（用户截图）**：遮罩透明度写成 1（全屏纯黑）→ 0.4；快照行无背景（非选中项黑字隐入黑遮罩）→ 加 colorBgContainer 底色；箭头 ▾ 换 ▼。真机验证：触发条显示、面板展开、选中回写「图片▾」✓
5. **上传进表单**：registerXFormProRenderer('uploadImage'/'uploadVideo') 注册（forms.tsx 模块级），XFormDemo 加 photos 字段（Form.Item + XUploadImage），XFormProDemo 加 photos 配置项 ✓
6. **桌面图标未更新**：用户重装的是旧 APK。资源已验证在位（webp 全替换）；需重新构建 APK（本次 gradle assembleDebug 已包含）+ 若 MIUI 桌面仍缓存旧图标需重启桌面/清除启动器缓存
7. **车牌键盘底部安全区**：panel 加 paddingBottom: max(insets.bottom, 10) ✓
8. **逐字签名存字无效（根因）**：非受控场景下 signatureValue 每渲染重置为空 → 存字写入即丢。修复：加 innerValue 兜底 state。真机验证：画字 → 存字 → 顶部字条显示字符缩略卡 + 书写区清空；完成 → 弹层关闭 + 示例下方「签名结果」白底图展示 ✓。普通签名示例同样加结果图展示。
9. **示例页硬编码白色背景在系统深色模式下穿帮**（Section 卡白底白字、Menu 触发条白底白字隐形）：examples/ui.tsx DemoPage/Section 改 useXTheme 主题自适应。**教训：demo 页面严禁硬编码颜色，一律 useXTheme**。

验证方式：adb install -r + adb reverse + Metro 热更新 + adb exec-out screencap 截图 + input tap/swipe 交互。注意：无 param 时 input tap 是 0ms 极速点按，React responder 的 grant 可能不触发，用 `input swipe x y x y 150` 模拟真人点击。
